// array mein aap different datatypes ke elements rakh sakte ho
// tuples mein aap fixed number of elements rakh sakte ho jinke types predefined hote hain

let arr1 : [string, number, boolean] = ["ankit", 12, true]; 
// if i write number first and then string it will show error

let arr2:[number, boolean] = [25, false];
